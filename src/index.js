import React from "react";
import ReactDOM from "react-dom";

const divElement = (
    <div>
        <h1>Tabassum Khan</h1>
        <button className ="btn btn-block btn-lg btn-outline-primary">You Borrowed</button>
    </div>
);

ReactDOM.render(divElement, document.getElementById("root"));